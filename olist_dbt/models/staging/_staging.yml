version: 2

sources:
  - name: bronze
    database: workspace
    schema: bronze

    tables:

      - name: orders
        columns:
          - name: order_id
            tests: [not_null, unique]

          - name: customer_id
            tests: [not_null]

      - name: customers
        columns:
          - name: customer_id
            tests: [not_null, unique]

      - name: order_items

      - name: order_payments

      - name: order_reviews

      - name: sellers
        columns:
          - name: seller_id
            tests: [not_null, unique]

      - name: products   
      
      - name: geolocation
      
      - name: category_translation